/**
 * Sprite Atlas Tool - Main Entry Point
 * @module main
 */

import { initUI } from './ui/uiController.js';

/**
 * Initialize the application when DOM is ready
 */
document.addEventListener('DOMContentLoaded', () => {
    initUI();
});
